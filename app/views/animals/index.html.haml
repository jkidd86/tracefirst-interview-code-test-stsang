%p#notice= notice
.row.mt-2
  .col-6
    %h2
      Animals
%br
%table.table
  %thead
    %tr
      %th Unique Tag
      %th Species
      %th Breed
  %tbody
    - @animals.each do |animal|
      %tr
        %td= animal.unique_tag
        %td= animal.species
        %td= animal.breed
        %td
          = button_to 'Show', animal_path(animal), method: :get
          = button_to 'Edit', edit_animal_path(animal), method: :get
          = button_to 'Destroy', animal_path(animal), method: :delete, data: { confirm: 'Are you sure?' }
%br
= link_to 'New Animal', new_animal_path

:css
  .button_to,
  .button_to div {
    display: inline;
  }